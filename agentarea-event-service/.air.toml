root = "."
tmp_dir = "/tmp/air"

[build]
  cmd = "go build -o /tmp/air/event-service ./cmd/server"
  bin = "/tmp/air/event-service"
  include_ext = ["go", "mod", "sum"]
  exclude_dir = ["vendor", ".git"]
  delay = 1000

[log]
  time = false

[misc]
  clean_on_exit = true
