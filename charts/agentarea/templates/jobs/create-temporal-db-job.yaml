{{- if and .Values.temporal.enabled .Values.postgresql.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "agentarea.fullname" . }}-create-temporal-db
  labels:
    {{- include "agentarea.labels" . | nindent 4 }}
    app.kubernetes.io/component: create-temporal-db
  annotations:
    # This job is a standard resource that waits for the database to be ready.
    # We use internal retry logic instead of Helm hooks to avoid dependency deadlocks.
spec:
  ttlSecondsAfterFinished: 300
  template:
    metadata:
      labels:
        {{- include "agentarea.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: create-temporal-db
    spec:
      restartPolicy: Never
      initContainers:
        - name: wait-for-db
          image: "postgres:alpine"
          imagePullPolicy: IfNotPresent
          command: ['sh', '-c', 'until pg_isready -h {{ include "agentarea.database.host" . }} -U $POSTGRES_USER; do echo waiting for database; sleep 2; done;']
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: "{{ .Values.global.secrets.postgresql }}"
                  key: username
      containers:
        - name: create-db
          image: "postgres:alpine"
          imagePullPolicy: IfNotPresent
          command: ["sh", "-c"]
          args:
            - |
              echo "Checking/Creating temporal database..."
              export PGPASSWORD=$POSTGRES_PASSWORD
              psql -h {{ include "agentarea.database.host" . }} -U $POSTGRES_USER -d postgres -tc "SELECT 1 FROM pg_database WHERE datname = '{{ .Values.temporal.database.name | default "temporal" }}'" | grep -q 1 || \
              psql -h {{ include "agentarea.database.host" . }} -U $POSTGRES_USER -d postgres -c "CREATE DATABASE {{ .Values.temporal.database.name | default "temporal" }};"
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: "{{ .Values.global.secrets.postgresql }}"
                  key: username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: "{{ .Values.global.secrets.postgresql }}"
                  key: password
{{- end }}
