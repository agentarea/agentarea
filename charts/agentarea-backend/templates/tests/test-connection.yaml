apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "agentarea-backend.fullname" . }}-test-connection"
  labels:
    {{- include "agentarea-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "agentarea-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
