{{- if .Values.kratos.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "agentarea.fullname" . }}-create-kratos-db
  labels:
    {{- include "agentarea.labels" . | nindent 4 }}
    app.kubernetes.io/component: create-kratos-db
  annotations:
    "helm.sh/hook": post-install,post-upgrade
    "helm.sh/hook-weight": "5"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  template:
    metadata:
      labels:
        {{- include "agentarea.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: create-kratos-db
    spec:
      restartPolicy: Never
      containers:
        - name: create-db
          image: "postgres:alpine"
          imagePullPolicy: IfNotPresent
          command: ["sh", "-c"]
          args:
            - |
              echo "Waiting for PostgreSQL to be ready..."
              export PGPASSWORD=$POSTGRES_PASSWORD
              until psql -h {{ .Release.Name }}-postgresql -U $POSTGRES_USER -d postgres -c "SELECT 1" >/dev/null 2>&1; do
                echo "PostgreSQL not ready yet, retrying in 5s..."
                sleep 5
              done
              echo "Checking/Creating kratos database..."
              psql -h {{ .Release.Name }}-postgresql -U $POSTGRES_USER -d postgres -tc "SELECT 1 FROM pg_database WHERE datname = '{{ .Values.kratos.database.name | default "kratos" }}'" | grep -q 1 || \
              psql -h {{ .Release.Name }}-postgresql -U $POSTGRES_USER -d postgres -c "CREATE DATABASE {{ .Values.kratos.database.name | default "kratos" }};"
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: "{{ .Values.global.secrets.postgresql }}"
                  key: username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: "{{ .Values.global.secrets.postgresql }}"
                  key: password
{{- end }}
