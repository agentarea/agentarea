FROM alpine:3.18

# Install ca-certificates for HTTPS
RUN apk add --no-cache ca-certificates

# Copy pre-built binary
COPY mcp-manager /usr/local/bin/mcp-manager
RUN chmod +x /usr/local/bin/mcp-manager

# Expose port
EXPOSE 8000

# Run
ENTRYPOINT ["/usr/local/bin/mcp-manager"]
