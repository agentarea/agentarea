// Moved to @agentarea/ui-shell so a second deployment can render the same shell.
// Re-exported from its original path so existing imports keep working.
export { useIsMobile } from "@agentarea/ui-shell";
