apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "agentarea-mcp-manager.fullname" . }}-test-connection"
  labels:
    {{- include "agentarea-mcp-manager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "agentarea-mcp-manager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
