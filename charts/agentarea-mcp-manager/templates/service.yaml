apiVersion: v1
kind: Service
metadata:
  name: {{ include "agentarea-mcp-manager.fullname" . }}
  labels:
    {{- include "agentarea-mcp-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agentarea-mcp-manager.selectorLabels" . | nindent 4 }}
