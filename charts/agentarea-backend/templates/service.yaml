apiVersion: v1
kind: Service
metadata:
  name: {{ include "agentarea-backend.fullname" . }}
  labels:
    {{- include "agentarea-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agentarea-backend.selectorLabels" . | nindent 4 }}
