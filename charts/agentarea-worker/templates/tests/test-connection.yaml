apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "agentarea-worker.fullname" . }}-test-connection"
  labels:
    {{- include "agentarea-worker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "agentarea-worker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
