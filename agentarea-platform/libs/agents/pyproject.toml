[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "agentarea-agents"
version = "0.0.12"
description = "Agent functionality for AgentArea"
license = {text = "Apache-2.0"}
requires-python = ">=3.12"
dependencies = [
    "agentarea-common",
    "a2a-sdk>=0.2.8",
    "pydantic>=2.4.2",
    "httpx>=0.25.0",
    "python-frontmatter>=1.1.0",
]

[tool.uv.sources]
agentarea-common = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["agentarea_agents"] 