[Unit]
Description=AgentArea sandbox container egress firewall (DOCKER-USER)
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
RemainAfterExit=true
ExecStart=/usr/local/sbin/sandbox-egress.sh

[Install]
WantedBy=multi-user.target
