apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "agentarea-secrets-manager.fullname" . }}-test-connection"
  labels:
    {{- include "agentarea-secrets-manager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "agentarea-secrets-manager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
