apiVersion: v1
kind: Service
metadata:
  name: {{ include "agentarea-secrets-manager.fullname" . }}
  labels:
    {{- include "agentarea-secrets-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agentarea-secrets-manager.selectorLabels" . | nindent 4 }}
