apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "agentarea-frontend.fullname" . }}-test-connection"
  labels:
    {{- include "agentarea-frontend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "agentarea-frontend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
