apiVersion: v1
kind: Service
metadata:
  name: {{ include "agentarea-worker.fullname" . }}
  labels:
    {{- include "agentarea-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agentarea-worker.selectorLabels" . | nindent 4 }}
