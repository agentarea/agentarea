# Environment-specific inventory (contains a concrete host). Commit only the example.
inventory.ini
*.retry

# Kubeconfig generated by the k3s play. Contains cluster-admin credentials for
# the execution cluster — never commit it.
*.kubeconfig
